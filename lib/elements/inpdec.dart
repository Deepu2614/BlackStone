import 'package:flutter/material.dart';

class Inpdec extends StatelessWidget {
  const Inpdec({super.key});

  @override
  Widget build(BuildContext context) {
    return InputDecorator(decoration: InputDecoration(

    ));
  }
}